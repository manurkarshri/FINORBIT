import test from "node:test"; import assert from "node:assert/strict"; import { reportToPdf, reportToSpreadsheetXml } from "../src/engines/export-engine.js";
const report = { generatedAt: "2026-09-06T10:00:00Z", filters: { dateFrom: "2026-09-01", dateTo: "2026-09-30" }, totals: { incomePaise: 100000, expensePaise: 25000, savingsPaise: 75000 }, rows: [{ date: "2026-09-05", type: "expense", amountPaise: 25000, account: "Bank", category: "Food", familyMember: "Asha", merchant: "Shop" }], byCategory: [{ category: "Food", amountPaise: 25000 }] };
test("PDF export is a complete paginated PDF byte stream", () => { const bytes = reportToPdf(report); const text = new TextDecoder().decode(bytes); assert.equal(text.startsWith("%PDF-1.4"), true); assert.match(text, /\/Type \/Page/); assert.match(text, /startxref/); assert.ok(bytes.length > 500); });
test("spreadsheet export contains structured transaction, summary, and category worksheets", () => { const xml = reportToSpreadsheetXml(report); assert.match(xml, /Worksheet ss:Name="Transactions"/); assert.match(xml, /Worksheet ss:Name="Summary"/); assert.match(xml, /Worksheet ss:Name="Categories"/); assert.match(xml, />25000</); });
